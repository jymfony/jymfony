/* eslint-disable no-unused-vars */

class StubbedTranslator {
    __construct(container) {

    }

    addResource(format, resource, locale, domain = null) {
    }
}

module.exports = StubbedTranslator;
