declare namespace Jymfony.Contracts.Logger {
    export enum LogLevel {
        EMERGENCY = 600,
        ALERT = 550,
        CRITICAL = 500,
        ERROR = 400,
        WARNING = 300,
        NOTICE = 250,
        INFO = 200,
        DEBUG = 100,
    }
}
